
let secrets = require('secrets')

let config = {
	port: 8899,
	imageDBpath: '/imageDB/',
	appId: "2710167052529715",
	...secrets
}

config.uri = `mongodb+srv://${config.mongodb.user}:${config.mongodb.pwd}@cluster0.pjx4h.mongodb.net/${config.mongodb.dbName}?retryWrites=true&w=majority`

// if(process.cwd().indexOf("wasteManagers") == process.cwd().length - "wasteManagers".length) config.imageDBpath = "wasteManagers/wasteBot"+config.imageDBpath

module.exports = config

